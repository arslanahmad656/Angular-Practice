import { TextTransformerPipe } from './text-transformer.pipe';

describe('TextTransformerPipe', () => {
  it('create an instance', () => {
    const pipe = new TextTransformerPipe();
    expect(pipe).toBeTruthy();
  });
});
