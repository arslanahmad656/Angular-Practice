import { ShowTwiceDirective } from './show-twice.directive';

describe('ShowTwiceDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowTwiceDirective();
    expect(directive).toBeTruthy();
  });
});
